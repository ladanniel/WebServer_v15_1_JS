继承：
选择器优先级：
不同选择器执行有限级：Id类>标签名>继承>默认效果（作用范围越小，优先级越高）
#常见错误
1、通过float浮动定位后的元素，不收纵向对其方式的影响，可以随意地调整外边距
2、行内或行内块元素，受纵向对其方式影响，不可以随意调整上下外内距；
3、通过相对定位改变元素的位置最简单快捷
4、如果元素内部的所有子元素全部浮动；则元素的高度为0，通过overfloat=hidden属性，就算子元素浮动了也能
   识别出内容的高度；
5、元素所有子元素，浮动后，高度为0